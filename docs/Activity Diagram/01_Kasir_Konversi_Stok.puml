@startuml
!theme plain
|Kasir|
start
:Buka menu Konversi Stok;
:Pilih produk & jumlah PCS;
:Pilih mode konversi;

|Sistem|
if (Mode Parsial?) then (Ya)
  :Cek buffer & hitung kebutuhan;
  :Update stok karton & buffer;
else
  :Konversi penuh ke PCS;
  :Kosongkan buffer;
endif

:Validasi stok & qty;
if (Valid?) then (Ya)
  :Update stok & catat log;
else (Tidak)
  :Tampilkan error;
  stop
endif

|Kasir|
:Tampilkan hasil konversi;
stop
@enduml
